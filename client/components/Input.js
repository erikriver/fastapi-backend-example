import styled from 'styled-components';

const Input = styled.input`
  height: 38px;
  margin-top: 5px;
  padding: 8px 6px;
  border-top: none;
  border-right: none;
  border-left: none;
  border-image: initial;
  border-bottom: 1px solid var(--warning-red);
  width: 100%;
  box-sizing: border-box;
  background-color: rgba(34, 31, 32, 0.035);
  font-size: 0.95em;
  font-family: BioSans-Regular, Roboto;
  color: var(--koffie-primary-black);
`;

export default Input;
